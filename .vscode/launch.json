{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "C++ Launch",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/output/main.exe",    // executável
            "args": [],                              // argumentos do programa
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",             // diretório de trabalho
            "environment": [],
            "externalConsole": false,                // console interno
            "MIMode": "gdb",
            "preLaunchTask": "build"                 // executa a task de build antes
        }
    ]
}
